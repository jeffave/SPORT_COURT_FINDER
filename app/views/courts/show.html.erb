<h1> <%= @court.name %> </h1>
<ul>
  <li> LOCALIZACÃO: <%= @court.location %> </li>
  <li> DISPONIVEL: <%= @court.availability ? "Disponivel" : "Não Disponivel" %> </li>
  <li> PREÇO: R$ <%= @court.pricing %> </li>
  <li> CAPACIDADE: <%= @court.capacity %> </li>
</ul>

<ul>
<% @reservations.each do |reservation| %>
  <% @users.each do |user| %>
  <% if reservation.user_id == user.id %>
  <li> <%= user.first_name %> booked here to <%= reservation.date %>
        <% if policy(reservation).destroy? %>
        <%= link_to "Delete",
            reservation_path(reservation),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
        <% end %>
  </li>
  <% end %>
  <% end %>
<% end %>
</ul>

<%= simple_form_for([@court, @reservation]) do |f| %>
  <%= f.input :date %>
  <%= f.submit %>
<% end %>

<% if policy(@court).edit? %>
  <%= link_to "Edit", edit_court_path(@court) %>
<% end %>

<% if policy(@court).destroy? %>
<%= link_to "Delete",
            court_path(@court),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
<% end %>

<%= link_to "Back to Courts", courts_path %>
